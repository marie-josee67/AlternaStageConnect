{% extends 'base.html.twig' %}

{% block title %}Ajouter une annonce{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <div id="headerPage" >
            <h1>Ajouter une annonce</h1>
            <a href="{{path('app_annonce')}}"> Retourner à la liste</a>
            
        </div>
    </div>
    <div class="container">
        <div class="row row-cols-1">
            {{ form(formCreate)}}
        </div>
    </div>
{# {{ dump(annonce) }} #}
{% endblock %}