{% extends 'base.html.twig' %}

{% block title %}Ajouter une alternance{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <div id="headerPage" >
            <h1>Ajouter une alternance</h1>
            <twig:BootstrapButton type="primary" path="{{('app_alternances')}}" label="Retourner à la liste"/>   
        </div>
    </div>
    <div class="container">
        <div class="row row-cols-1">
            {{ form(formCreate)}}
        </div>
    </div>
    {# {{ dump(request )}}  #}

{% endblock %}